name: HQA Validation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install Poetry
      run: |
        curl -sSL https://install.python-poetry.org | python3 -
        echo "$HOME/.local/bin" >> $GITHUB_PATH

    - name: Install Python dependencies
      # `dev` extras pull in pytest + pytest-benchmark for the bench step.
      run: poetry install --no-root --extras dev

    - name: Set up Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Run Rust Tests (Validation)
      run: cargo test --release -- --nocapture

    - name: Build and install Rust extension
      run: |
        pip install maturin
        poetry run maturin develop --release

    - name: Run Python Benchmarks/Tests
      run: poetry run pytest dse_pau/bench_python_hqa.py
